# 仓库指南

## 项目结构与模块组织
本仓库包含 MATLAB 分析文件以及 A 题相关的竞赛/数据附件。除非项目规模增长到需要单独分目录，否则请将源码脚本保存在仓库根目录。

- `problem2.m`：当前优化流程的主 MATLAB 脚本。
- `problem2.mlx`：用于交互式探索/报告的 MATLAB Live Script 配套文件。
- `plot_histogram.m`：用于绘制 24 小时曲线的辅助脚本。
- `附件*.xlsx`：输入数据集和参数表；请将其视为只读源数据。
- `problem1(1).pdf`：题目说明/参考文档。

如需添加输出文件，建议新建 `outputs/` 目录，用于存放生成的图像、表格或报告。除非确有必要，请避免提交临时 MATLAB 文件。

## 构建、测试与开发命令
本项目没有编译构建步骤。请使用 MATLAB 运行或验证脚本：

- `matlab -batch "run('problem2.m')"`：以非交互方式运行主优化脚本。
- `matlab -batch "run('plot_histogram.m')"`：在填充所有曲线数组后检查绘图脚本。
- `git status`：在提交前检查暂存区/未跟踪文件的变更情况。

优化脚本使用了 `optimoptions`、`ga` 等 MATLAB 函数；请确保已安装 Global Optimization Toolbox（全局优化工具箱）。

## 代码风格与命名规范
请保持一致的 MATLAB 代码风格：

- 在函数、循环和续行代码块中使用四个空格缩进。
- 使用具有描述性的全小写变量名；必要时添加题号前缀，例如 `problem2_load`、`problem2_buycost`。
- 将可复用计算放在 `.m` 文件末尾的局部函数中，例如 `objFun`。
- 保持中文注释和标签使用 UTF-8 文本编码；避免混用编码或出现乱码。

## 测试指南
当前没有自动化测试套件。修改后，请在 MATLAB 中运行受影响的 `.m` 或 `.mlx` 文件，并检查图像、打印指标和优化结果是否合理。对于数值变化，请在提交或 PR 描述中记录关键输出值，方便审阅者比较行为变化。

## 提交与拉取请求指南
近期提交信息使用了简短、聚焦任务的写法，例如 `problem2`、`problem2(1)` 和 `Add MATLAB histogram plotting script with x-axis 0-23`。建议使用简洁的祈使句提交信息，并明确指出修改的问题或脚本。

拉取请求应包含简要摘要、变更文件、已运行的命令以及生成的图像/结果（如有）。如有关联 issue，请添加链接，并注明所需工具箱或数据文件。
